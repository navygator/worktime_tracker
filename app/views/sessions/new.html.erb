<% provide(:title, "Sign in") %>
<h1>Sign in</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(:session, url: sessions_path) do |f| %>
        <%= render 'login_fields', f: f %>
    <% end %>
    <p><%= link_to "Request access", "#" %></p>
  </div>
</div>
